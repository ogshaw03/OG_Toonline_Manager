# HANDOFF — OG Toonline Manager（実機検証の引き継ぎ）

Claude Code on the web（実機なし）での作業を、**Maya 実機（VP2/DirectX11）のあるローカルセッション**へ
引き継ぐためのメモ。ビューポート依存の輪郭機能は web 側では動作確認できないため、実機で検証・調整する。
設計の詳細は **`CLAUDE.md`**（各機能の節）に集約済み。本ファイルは「今どこが未検証で、何を疑い、どう直すか」に絞る。

---

## 0. リポジトリ / 環境

- **repo**: `ogshaw03/OG_Toonline_Manager`
- **dev ブランチ**: `claude/repository-handoff-review-k54kwk`（全実装がここ。`git pull` すれば最新）
- **配布/更新**: `OG_Toonline_Manager_install.py` を Maya ビューポートにドラッグ（ホットアップデート運用。
  詳細は CLAUDE.md「配布・ホットアップデート」）。ローカル反復は環境変数 `OG_TOONLINE_MANAGER_USE_LOCAL=1`。
- **前提**: Maya 2022+（Python3）／ VP2 = **DirectX11** ／ ビューポート「テクスチャ表示 ON（6キー）」。

---

## 1. 現状サマリ（動く / 未検証）

### 概ね安定（実機で良好と報告済み）
- スクリーン輪郭の**太さ**（一定px・近接/遠方で変わらない・距離で消えない）
- スクリーン輪郭が**重なり部にも線**を出す（深度シルエット）

### 実機で未検証・要調整（web 側で盲目実装した部分）
1. **スクリーン輪郭の曲率**（頂点カラー COLOR0 駆動）→ 「効かない」報告あり
2. **スクリーン輪郭 滑らか面のジャギ**（被覆境界の階段状）→ 高分割しないと綺麗にならない
3. **画像空間エッジ検出（オブジェクト単位）** → 背景黒は修正済み、**線がかすれる**（未解決）
4. **スクリーン輪郭 深度バイアスのビュー空間化**（`gProj` 使用）→ 太さ回帰は修正済み、深度精度は要確認

---

## 2. 各項目：第一容疑と対処

### 1) 曲率が効かない（COLOR0 バインド）
- 実装: `_SCRN_FX_HLSL` の `COLOR0.r` を太さ push に乗算。`_init_scrn_color_set`（色セット `toonScrnCurv`）を
  **シェーダ割り当ての前**に作成、`_update_scrn_curv_weights`（om2 `setVertexColors`）で R に曲率倍率を書く。
- **疑い**: dx11Shader が COLOR0 にこの色セットを流していない。
- **確認/対処**: dx11Shader のアトリビュートエディタで頂点入力（Vertex Elements）マッピングを見て、
  `toonScrnCurv` を COLOR0 に明示バインド。切り分けは PS で `return float4(IN.Color.rrr, 1)` にして
  曲率カラーが出るか。色セットが deformer 下流の polyColorPerVertex で保持されているかも確認。

### 2) 滑らか面のジャギ（構造的）
- 幾何式スクリーン輪郭の被覆境界は**原理的にポリ数依存**（シェルと元メッシュの深度比較境界が多角形）。
- **対処**: MSAA（Viewport 2.0 の Anti-aliasing）で緩和。**解像度非依存の滑らかさが要るなら (3) に寄せる**。

### 3) 画像空間エッジ検出のかすれ ← ビューポート用途の本命
- 実装: `_edge_enable` の `_EdgeOverride`。2パス構成 = ①全シーンを色 `tColor` へ ②`objectSetOverride` で
  **対象メッシュだけ**を深度 `tDepth`（専用スクラッチ色 + ALL クリア）へ → フルスクリーン quad が
  `tDepth` の深度ラプラシアン（`_EDGE_FX`）で段差を検出し `tColor` に合成。**線は対象にだけ乗る**（オブジェクト単位OK）。
- **疑い**: (a) **HiDPI**：`setup()` の `MRenderer.outputTargetSize()` が論理サイズを返しターゲットが実ピクセルの
  半分→拡大ボケでかすれ。実ピクセルで作る。(b) **深度 z 非線形**：房どうしの深度差が小さく段差が弱い→
  near/far uniform を渡して**線形化**。
- **確認**: `_EDGE_FX` を `return float4(c,c,c,1)`（c=深度）にして深度が段差として見えるか。ターゲット w/h をログ。

### 4) 深度バイアス（gProj / ビュー空間）
- VS: `clip = pos*gWVP`（**xy/w は不変**＝太さ補正）、`clip.z` だけ `vpos.z-=depthBias → gProj` で差し替え。
  `clip.w` を触ると近接で線が消える/太さが距離変化（踏んだ罠）。UI「重なり深度(スクリーン)」で符号/量調整。
- **疑い**: `gProj`（Projection semantic）の行/列規約。被覆や深度が変ならここ。
- **戻し方**: 崩れたら該当コミットを revert（`b38e086`/`1f0f7d9` 付近）で旧 `gZBias`（clip 空間NDC一定）方式へ。
  旧方式は太さ安定・被覆綺麗だが「遠距離で重なり線が消える」弱点あり。

---

## 3. 方針の推奨

- **ビューポート/リアルタイムで、滑らか面でもポリ数に依存せず綺麗**にしたい → **(3) 画像空間エッジ検出**が本命。
  実機で①解像度（HiDPI）②深度線形化 を直すのが最優先。これが決まれば (1)(2)(4) の悩みの多くが不要になる可能性。
- 幾何式スクリーン輪郭 (1)(2)(4) は MSAA 併用で実用範囲を探る用途。

---

## 4. 触ってよい/注意

- 破壊的 git 操作（force push / main 直 push / ブランチ削除）はユーザー確認必須（CLAUDE.md ブランチ運用）。
- 単一ファイルツール。`.py` を分割したら install.py を `_REMOTE_FILES` 方式へ拡張（CLAUDE.md §1-10）。
- 実機で直したら **`__version__` を上げて push** → ユーザーは「GitHub から更新」で取り込める。
